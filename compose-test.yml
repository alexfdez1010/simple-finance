services:
  db:
    image: ankane/pgvector
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=db
      - PGPORT=5434
    container_name: postgres_simple_finance_test
    ports:
      - '5434:5434'
